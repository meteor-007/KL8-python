# 全子系统缺陷落地实现日志（2026-08-06）

> 目标：`/goal 利用skills和mcp的能力全部落地实现` — 将《分析子系统深度报告_2026-08-06.md》中识别的 P0/P1/P2 缺陷全部落地修复。
> 执行方式：11 个子系统分两波并行（Wave1=6 系统，Wave2=5 系统），每波由独立实现 agent 按"读代码→改→逐项验证"执行。
> 约束：不提交 git；只改本子系统目录；保持 CLI/API/输出格式兼容；不跑完整回测（轻量冒烟验证）。

---

## 一、全局验证证据（两波合并）

### 1.1 语法编译（py_compile）
- **Wave1 全部 43 个改动 .py → 全部通过**
- **Wave2 全部 43 个改动 .py → 全部通过**
- `kl8-pwa/static/js/sw.js` → `node --check` 通过

### 1.2 模块导入（项目根目录，跨子系统）
| 子系统 | 导入结果 |
|---|---|
| gemini选2-预测 | 6 模块 OK（k8_quant_engine/auto_evolve/validation/online_learn/backtest_features/daily_run） |
| 定金选2-分析 | 7 模块 OK（含 config.scoring_config、learning.autonomous_learner） |
| 数据汇总复盘 | 7 模块 OK（aggregate_v18/evolution_engine/backtest_v91 等） |
| 跟随分析 | 10 模块 OK（含 app） |
| 重点点位分析 | 9 模块 OK；唯一失败项 = `optuna`（本机未安装的可选依赖，非本次改动引入） |
| 双层LSTM | 13 模块 OK（含 experiments） |
| 顺口溜 | 6 模块 OK（含 c_engine.formula_match_c） |
| KillSeeker / 点位期数-追踪 / data/ / kl8-pwa | py_compile + 冒烟通过 |

### 1.3 运行时冒烟
- kl8-pwa Flask 应用 `GET /` → **200**
- 跟随分析 `FEATURE_DIM = 23`（与 index.html 文案统一后一致）
- 双层LSTM `next_period('2026-12-31','2026351') = 2027001`（跨年正确）、同年 `2026352`
- 重点点位分析加载 best_params.json 时 70 天陈旧警告**正确触发**（P1-8 生效）
- data/ core.signal_registry 模块级函数可正常导入

---

## 二、Wave1 修复明细（6 子系统）

### 2.1 KillSeeker（5 文件）
| # | 修复项 | 文件 |
|---|---|---|
| P1 | 遗漏序列 `omission_series[:50]` → `[-50:]`（取最近 50 期而非最旧） | core/curve_analyzer.py |
| P1 | 删除伪 Layer-3 趋势预测（layer3_data 恒空） | core/curve_analyzer.py |
| P1 | 杀号回退逻辑尊重 max_per_decade 上限 | core/kill_predictor.py |
| P1 | 跨文件解析容错：`\d{4,}` 长数字清洗、定位正则 `(?<!\d)` 边界、`\[([^\]]+)\]` 提取 | core/cross_feed.py |
| P1 | 冷区密度标注 | core/density_detector.py |
| P2 | run_diagnose 增加 numpy/scipy 可用性检查 | main.py |

### 2.2 双层LSTM（12 文件 + 2 新增）
| # | 修复项 | 文件 |
|---|---|---|
| P0 | 训练/验证分割 85/15 无数据丢弃；每折独立种子；排序损失改为 logit 尺度 ranking loss | core/trainer.py |
| P0 | 删除假"additive attention"分支 | core/lstm_model.py |
| P0 | 关闭 range-gate 并文档化（保留接口） | core/seed_ensemble.py |
| P1 | 模型加载校验 11 个架构字段（ckpt['config']） | core/predictor.py |
| P1 | 下一期号由日期推断（next_period），修复 int+1 跨年错误 | main.py / precheck.py / core/period_utils.py(新) |
| P1 | 统一 set_seed / hypergeom_tail_pvalue | core/utils.py(新) |
| P2 | SEARCH_SPACE 移除 "additive" | auto_search.py |
| P2 | 实验脚本统一 import | experiments/run_phase2.py 等 3 文件 |

### 2.3 顺口溜（6 文件）
| # | 修复项 | 文件 |
|---|---|---|
| P0 | C 规则陈旧检测 + 自动再生成（SK_AUTO_REGEN_C=1）；层级标签用真实投注数 | c_engine/formula_match_c.py |
| P0 | 删除 602 行 ABC 死代码，重写 main() | c_engine/formula_match_c.py |
| P1 | 挖掘 Top-N 堆（_MINE_TOP_N=300）+ 期序 tiebreaker 修复（原 TypeError）；候选 -60% 且 Top-90 一致 | c_engine/generate_c_docs.py |
| P1 | 数据加载逐行 try/except（坏行跳过）；路径改环境变量/相对定位 | data_loader.py |
| P1 | coappear_give_or 置信度按预测数均分；删除 skip2_chain 死回退 | rule_engine.py |
| P2 | 二选一标签修正 | daily_predict.py |

### 2.4 kl8-pwa（4 文件）
| # | 修复项 | 文件 |
|---|---|---|
| P0 | **子进程超时永不触发**（阻塞 stdout 读至 EOF 后才 wait）→ 读取线程 + proc.wait(timeout) + 进程树强杀 | app.py |
| P0 | step_start_time 字符串键兼容 | app.py |
| P1 | MAX_SESSIONS=50 + MAX_LOG_LINES=2000 + 最旧驱逐 | app.py |
| P1 | Token 认证（env/token.txt + X-Api-Token/?token= SSE） | app.py |
| P1 | SSE 加锁、utf-8-sig 优先、manifest mimetype | app.py |
| P2 | SW 缓存清单移除不存在资源、CACHE_NAME v2 | static/js/sw.js |
| P2 | STEP2_LABELS→12、STEP5_LABELS→5、apiFetch 封装 | templates/index.html |

### 2.5 点位期数-追踪（7 文件）
| # | 修复项 | 文件 |
|---|---|---|
| P0 | **前视泄漏**：`point_status.pop(T, None)` 常清（look-ahead 修复） | main.py / 每日分析.py |
| P0 | 遗漏/断层检测识别期号缺口→警告+跳过 | tracking.py |
| P1 | repeat_candidates 收紧为"T-1 开出且 T-2/T-3 均未开出"（46→13） | tracking.py |
| P1 | 删除伪造随机基线 0.583 → 经验值 0.605 | backtest.py |
| P2 | Excel 列名 >26 列支持；死代码清理 | excel_report.py / point_map.py |

### 2.6 data/ 共享数据层（14 文件）
| # | 修复项 | 文件 |
|---|---|---|
| P0 | **BAYES/MC 通道修复**：top20 恒为 [1..20] 的系列 bug → posteriors/sim_freq + 回退键 | core/signal_registry.py / main_v2.py |
| P0 | draw_order 字段贯通 | utils/history_loader.py / core/deep_optimizer.py |
| P1 | 特征优化单路径（plan19 过滤原被跳过） | core/feature_optimizer.py |
| P1 | 基线 history_only=True；共现基线 (20/80)²≈0.0601 | core/baseline.py / deep_mining_engine.py |
| P1 | 冷热命中精确率 vs 覆盖率区分；关联分析 prev/prev_prev 仅 gap=1 时用 | core/hotcode_hit_analysis.py / core/deep_association_analyzer.py |
| P1 | Lift 阈值统一 1.0（学习闭环） | learning/autonomous_learner.py / main_v2.py |
| P2 | O(n²)→单次全量缓存；废弃策略存根标注 | excel_deep_mining_v2.py / core/strategy_optimizer.py |

---

## 三、Wave2 修复明细（5 子系统）

### 3.1 gemini选2-预测（6 文件）
| # | 修复项 | 文件 |
|---|---|---|
| P0 | **选号口径漂移**：抽出共享 `select_lineup()`（生产 build_prediction 同款跨区跨尾+杀号过滤），auto_evolve/validation/backtest_features 复用，输出逐字段一致 | k8_quant_engine.py / auto_evolve.py / validation.py / backtest_features.py |
| P0 | 进化采纳门控 CI 改用进化最优权重的验证折命中 | auto_evolve.py |
| P0 | SEARCH_SPACE 移除死参数 canyon_min + 10 个 Loss 权重（维度 37→26） | auto_evolve.py |
| P0 | 金胆跳过被杀号 | k8_quant_engine.py |
| P1 | 默认路径不再整跑 GA（改 run_quick_checks，`--evolve` 显式触发） | daily_run.py |
| P1 | 在线学习回归门控（新权重金胆不劣于旧权重才写回） | online_learn.py / daily_run.py |
| P1 | backtest_features 窗口 <40 break；--online-selfcheck save_demo=False | backtest_features.py / daily_run.py |
| P1 | CI 统一 Wilson 口径；kill 语义统一以 kill_zones 为单一事实源 | daily_run.py / k8_quant_engine.py |
| P2 | 死代码清理；路径集中化（Path(__file__) + 环境变量） | k8_quant_engine.py 等 |

### 3.2 定金选2-分析（7 文件）
| # | 修复项 | 文件 |
|---|---|---|
| P0 | **DataLoader 坏行静默截断全库** → 逐行解析+计数告警 | data_loader.py |
| P0 | **Momentum 温号池恒 0.5** → 重写为遗漏变化率（始终有效） | model_engine.py |
| P0 | CONTEXT_MEMORY.md 按 (period, 类型) 去重 | main_predictor.py |
| P0 | data_cache.json fallback 默认禁用（环境变量门禁 + 警告） | data_loader.py |
| P1 | learning/ 闭环 → **V5.8.3 已正式接线**（见 §6.4：LEARNING_LOOP_ENABLED=True + 复盘后调用 + review_engine 跳过未开奖期） | main_predictor.py / learning/autonomous_learner.py / learning/review_engine.py |
| P1 | 盲测与生产权重路径统一（param_store） | rolling_backtest.py |
| P1 | 期号推进跨年修复（日期+1 推断，回退 int+1 带注释） | data_loader.py / model_engine.py / main_predictor.py |
| P1 | co_frequency 最小样本贝叶斯收缩（base_count<5 → 向 0.25 平滑） | model_engine.py |
| P1 | graph_coupling + co_frequency 合并为单一共现信号（权重归一） | model_engine.py |
| P2 | 死代码清理；版本号统一 5.8.3；热号输入逐 token 容错 | 多文件 |

### 3.3 数据汇总复盘（8 文件）
| # | 修复项 | 文件 |
|---|---|---|
| P0 | backtest_v10 相位图谱空壳 → [DEPRECATED] 标注 | backtest_v10.py |
| P0 | date_to_next_period V17/V18 语义统一（该日期期号） | aggregate_predictions.py |
| P0 | **删除 V17 伪造推荐**（任意数字抓取）；**p_value 改为真实二项检验**（binomtest vs 0.25，样本<30 返回 1.0） | aggregate_predictions.py / aggregate_v18.py |
| P0 | generate_review_excel 分区文件缺失→显式警告+跳过 | generate_review_excel.py |
| P0 | evolution_engine 路径改 config/paths_v18 | evolution_engine.py |
| P0 | **进化漂移去重**（review_dedup.json 按 period→日期一天一次）+ 0/0 空样本不重置 | evolution_engine.py |
| P1 | Watch5 改用 watch 号码字段 limit=5；deep_diagnosis2 records 修复 | aggregate_predictions.py / deep_diagnosis2.py |
| P1 | hit-detail 报告文件冲突（_legacy 后缀）；LSTM 正则 \uFFFD 修复 | generate_subsystem_reports.py / aggregate_v18.py |
| P1 | 冻结面板回退不再重算（无面板返回 none）；backtest_v91 前视声明 + <30 期防护 | aggregate_v18.py / backtest_v91.py |
| P2 | 共识按系统去重；get_records_by_period O(1) 索引；文档同步 | aggregate_v18.py |

### 3.4 跟随分析（14 文件）
| # | 修复项 | 文件 |
|---|---|---|
| P0 | **follow_rank_oc 恒 0 死特征** → 重写为候选×锚点源共现强度排名（预计算全 80 候选） | core/rf_features.py |
| P0 | **pair_lift 对非锚点候选结构性 0** → 候选×锚点源全量组合（80×20） | core/rf_features.py |
| P0 | **位置漂移动量方向取反** `pos_list[-5:]` → `[:5]` | core/features.py |
| P0 | 推演统计窗与遗漏/特征窗口一致（同一 window） | core/predict_top6.py |
| P0 | Web "树 undefined" → as_dict 补 n_estimators | core/rf_model.py |
| P1 | 特征维度文案统一 23 维；死代码清理（RANDOM_TOP6_EXPECTED/recent()/rrf_from_rank_maps 等） | templates/index.html / 多文件 |
| P1 | 重复回测逻辑委托 core；遗漏 clamp 两套实现统一引用 omission.py | scripts/hit_rate_analysis.py / core/features.py |
| P1 | 长度校验 + 错误记录（不再静默 break）；RF 排除锚点当期号码；prob_map 键统一 int | core/repeat_analysis.py / core/rf_model.py / app.py |
| P2 | 特征预计算共享（_precompute_follow_ranks）；requirements 补 numpy/scikit-learn | core/rf_features.py / requirements.txt |

### 3.5 重点点位分析（12 文件）
| # | 修复项 | 文件 |
|---|---|---|
| P0 | **backtest_rolling 完全不可运行**（_create_gbdt 不存在/phase 缺失/训练集含当期）→ 本地 helper + phase 字段 + 训练区间不含当期 | backtest_rolling.py |
| P0 | 区域基线全仓统一 0.5835（引用 config ZONE_BASELINE） | backtest_rolling.py |
| P0 | **实现真 OOF-LR 元学习器**（TimeSeriesSplit OOF 训 LR 组合权重，样本<40 回退加权平均） | model_engine.py |
| P0 | **真实二项检验**（binomtest vs 0.5835 greater；免死金牌标注"启发式标记非统计检验"） | model_engine.py |
| P0 | 闭环学习未接线 → **V6.3 已正式接线**（见 §6.3：model_engine._tuned 消费 + learner 真实写 store） | learning/autonomous_learner.py / model_engine.py / daily_report.py |
| P1 | 回填前瞻 → _compute_reverse_coverage_decades 加 max_period 过滤 | generate_historical_logs.py / model_engine.py |
| P1 | 历史模拟日志独立文件（不再污染 prediction_logs.txt） | generate_historical_logs.py / config/paths.py |
| P1 | Optuna 固定 seed=42 + 窗口 150 与生产一致 + best_params 陈旧警告 | optuna_optimizer.py / model_engine.py |
| P1 | data_loader 逐行异常防护；死代码清理 | data_loader.py / 多文件 |
| P2 | PRD 漂移标注（16 处 ⚠️，仅标注不重写） | docs/重点点位分析_PRD.md |

---

## 四、重要行为变更提示（需用户知晓）

1. **gemini选2-预测**：默认 `python daily_run.py` 不再跑 GA（原极慢全量进化），GA 需显式 `--evolve`；在线学习写回权重前有回归门控。
2. **定金选2-分析**：data_cache.json 回退默认禁用（需 `KL8_ALLOW_DATA_CACHE_FALLBACK=1`）；动量因子语义从"近3期出现"改为"遗漏变化率"。
3. **跟随分析**：位置漂移动量方向修正（原方向反了）；follow_rank_oc / pair_lift 从恒 0 变为真实信号——**模型特征分布变化，若需稳定产出建议重训 RF**。
4. **重点点位分析**：融合从固定权重改为 OOF-LR 元学习器；p 值从"当日排名伪 p"改为对 0.5835 基线的真实二项检验；backtest_rolling.py 恢复可运行。
5. **数据汇总复盘**：显著性星标现在基于真实二项检验（大宗共识不再是"必然显著"）；V17 伪推荐采集已删除。
6. **双层LSTM**：range-gate 已禁用（保留接口）；训练分割 85/15 不再丢弃数据。
7. **顺口溜**：C 规则陈旧时自动再生成（SK_AUTO_REGEN_C=1）。

## 五、遗留项（未做/需用户决策）

- **重点点位分析**是否继续使用（DEPRECATED.md 曾宣布废弃）→ 属用户决策，本次未改动系统存废。
- **len(window)<40 保护**使 gemini 短历史（<40 期）回测样本为 0 — 属预期，短历史本就不足以评估。
- 重点点位免死金牌标注仍为启发式（非统计检验）— 已如实标注，未夸大。

---

## 六、闭环接线与验证收尾（本会话追加）

### 6.1 依赖落地
- `pip install optuna`（4.9.0）完成；scipy 1.17.1 / scikit-learn 1.9.0 已存在。
- **真实运行时验证 optuna_optimizer.py**（20 trials，150 期窗口，seed=42 可复现）：最佳 OOF 得分 0.5487，成功产出 best_params.json，生产 model_engine 加载新参数（n_estimators=150）且陈旧警告不再触发。

### 6.2 修复 best_params 路径分叉 BUG（运行时验证发现）
- 原 `optuna_optimizer.py` 写入子系统根目录 `best_params.json`，而生产 `model_engine` 经 `config.paths.BEST_PARAMS` 读取 `config/best_params.json` → **生产环境从未拿到寻优结果**。
- 已改为写入 `config.paths.BEST_PARAMS`（生产唯一读取位置），删除根目录孤立文件。

### 6.3 重点点位分析 — learning 闭环正式接线（原 [未接线]）
- `model_engine.py` 新增 `_tuned(category, key, default)` 懒加载 helper（读 parameter_store，异常回退默认），4 处接线：walk_forward n_splits、ranking 5 维权重、fdr_q_threshold、tanh_boost_positive/negative + tanh_scale。
- `autonomous_learner.py`：self_adjust 真实写入 store（`wired: True`，递增 total_adjustments/last_adjustment_date），累积钳制改用 `DEFAULT_PARAMS` 为基线（±50% 红线四）；self_optimize 真实写入 tanh 增益（原为死写）。
- `daily_report.py` line 367 [未接线] 注释 → "V6.3 已接线"。

### 6.4 定金选2-分析 — learning 闭环正式接线（原 LEARNING_LOOP_ENABLED=False）
- `main_predictor.py`：`LEARNING_LOOP_ENABLED = True`，learner 调用移至 `automated_backtest` 之后（复盘后调用，匹配"复盘→学习"语义）。
- `parameter_store.py` DEFAULT_PARAMS 权重与生产统一：`graph_coupling=0.431`（V5.8.3 共现信号合并），`co_frequency=0.000` 保留兼容键；`autonomous_learner.py` 紧急重置权重同步 0.431。
- `review_engine.py`：`_load_predictions` 在 period=None 时跳过"尚未开奖期"的预测（修复取到未来期号导致报错）。
- 真实 main_predictor 冒烟：`✅ 闭环学习已调整 2 项参数（下次运行生效）`。

### 6.5 修复 learning 计数器未持久化根因（本轮验证发现）
- **根因**：`定金选2-分析/learning/parameter_store.py` 的 `get()` 在 category 级返回**活引用**；三个 learner 先原地修改 `learning_state` 再调 `update()`，导致 `update()` 检测 `old==new` → 不写 history、不 save。只有同轮后续 weights 变更触发的 save() 顺带持久化了前置修改（故 total_reviews 有值、total_adjustments 恒 0）。
- **修复**：`get()` category 级改为返回 `copy.deepcopy`（与重点点位分析既有实现一致）。
- **验证**：受控单测 3 项全过（update 返回 True / 值持久化 / history 写入 learning 条目）；生产冒烟后 counters 真实递增：`total_reviews 2→3, total_adjustments 7→8, total_optimizations 0→1`，history 出现 `category:learning` 条目；self_optimize 紧急重置（consecutive_low=3 且 hit_rate<20%）如设计触发。
- 歉意陈述：历史 7 次 self_adjust 运行计数器冻结为 0，已按 history 对账回填 total_adjustments=7（reason=reconcile 留痕）。

### 6.6 重点点位分析计数器一致性核对
- `total_reviews=26, total_optimizations=23, total_adjustments=0` — **一致**：该子系统 get() 本就返回深拷贝，persistence 从未损坏；total_adjustments=0 是因为 26 轮复盘中无调整提案（Level≥3 降级保护或不由 proposals），优化计数则来自连续低命中 tanh 阻尼触发（`consecutive_low>=5`）。

---

## 七、每日幂等校验：子系统「今日已执行」跳过/重跑（本会话追加）

### 7.1 功能说明
- 需求：每个子系统当天已执行过时，给出校验并提示「直接跳过 or 重新执行」。
- 已确认决策：**非交互模式默认跳过 + `KL8_FORCE_RERUN=1` 强制重跑**；覆盖**全部 11 个每日入口** + 调度器 `run_today_v18.py --force` 透传。
- 新增共享模块 `daily_run_guard.py`（仓库根）：
  - 状态文件 `daily_run_state.json`（仓库根，与 `k8_quant_memory.json` 并列；`KL8_RUN_STATE_FILE` 可覆盖路径便于测试）。结构 `{"2026-08-06": {"系统名": {"period", "ran_at", "status": "running|ok"}}}`，键 = 系统本地日期。保存时自动清理 31 天前记录。
  - `guard_daily_run(system, period=None, interactive=None) -> bool`：返回 `True`=跳过。逻辑：① `KL8_FORCE_RERUN=1` → 强制重跑；② 今日无记录或 `status!="ok"`（崩溃残留 running）→ 放行；③ 今日已有 ok → 交互终端弹 `[Y=跳过/N=重新执行](默认Y)`，非交互（无 tty 或 `KL8_NON_INTERACTIVE=1`）默认跳过。
  - `mark_daily_run_done(system, period=None)`：成功收尾置 `ok`，崩溃进程留下 `running` 残留 → 下次自动放行。
  - `python daily_run_guard.py --status` 查看今日各系统执行状态。
  - 所有 import 均 try/except 包裹，失败静默放行，绝不阻断子系统主流程。

### 7.2 接入清单（11 入口 + 1 调度器）
统一模式：入口顶部将仓库根加入 `sys.path`（跟随分析在 `scripts/` 上溯 3 层，其余 2 层）→ try-import guard → 参数解析后、重计算前调 `guard_daily_run`（命中即 return）→ 每日成功收尾处 `mark_daily_run_done`。回测/补跑/诊断模式不触发：

| 子系统/入口 | guard 位置（仅每日模式） | mark 位置 |
|---|---|---|
| KillSeeker/main.py | 数据新鲜度检查后、`if args.backtest` 前（排除 `--backtest/--as-of`） | "✅ KillSeeker V1.0 执行完毕" 前 |
| 双层LSTM/main.py | dispatch 中 `predict`/`full` 分支调用 cmd 前 | cmd_predict/cmd_full 返回后 |
| 顺口溜/daily_predict.py | 新鲜度检查后（排除 `--until-period` 补跑） | 报告写入后 |
| 点位期数-追踪/每日分析.py + main.py | 载入数据算出 T 后、`build_full_analysis` 前（排除 `--period` 补跑） | 报告/控制台输出后 |
| data/main_v2.py | `else:` 每日 pipeline 分支开头（排除 diagnose/oos/learn/validate/weekly/backtest） | pipeline + 每日报告生成后 |
| gemini选2-预测/daily_run.py | 各工具分支 return 之后、`date_key=...` 前（排除 `--backfill/--detect-gap/...` 等） | `run_one` 完成后 |
| 定金选2-分析/main_predictor.py | `ensure_dirs()` 后、`automated_backtest` 前；`interactive = not args.no_interactive` | 预测日志归档后 |
| 数据汇总复盘/aggregate_v18.py | `args.period is None`（今日聚合；`--period` 断档补跑不算） | 三系统共识聚合完成后 |
| 跟随分析/scripts/daily_run.py | STEP0 环境预检后、重计算前 | 报告写入后 |
| 重点点位分析/main_predictor.py | 数据加载前（`parse_kl8_history` 前） | 预测日志持久化后 |
| 数据汇总复盘/run_today_v18.py | 新增 `--force`：子进程 env 设 `KL8_FORCE_RERUN=1`；**始终**设 `KL8_NON_INTERACTIVE=1`（调度器绝不阻塞） | — |

- kl8-pwa 是服务端不加入口校验（已确认）；其触发的子进程自带校验（无 tty → 默认跳过）。

### 7.3 验证证据
1. `python -m py_compile` 全部 13 个改动文件 ✅。
2. guard 模块单测 8/8 通过（临时 `KL8_RUN_STATE_FILE`）：首跑放行 → running 残留放行 → ok+非交互跳过 → `KL8_FORCE_RERUN=1` 放行 → 交互 Y 跳过 → 交互 N 重跑 → `--status` 输出 → state 文件结构正确。
3. 冒烟（`点位期数-追踪/每日分析.py`，临时状态文件）：第一次正常运行产出选号；第二次打印 `⏭️ [每日校验] 点位期数-追踪 今天 (2026-08-06) 已执行过（期号 2026208）... 默认跳过` 并退出；`KL8_FORCE_RERUN=1` 第三次强制重跑正常执行。
4. `run_today_v18.py --help` 正确显示 `--force` 帮助；代码确认 `run_system` 始终设 `KL8_NON_INTERACTIVE=1`、`force=True` 时追加 `KL8_FORCE_RERUN=1`。
---

## 八、PWA v3.0 一键全自动流水线（本会话追加）

### 8.1 背景
原 PWA v2 为 6 步手动流程（确认点位 → 输入 Gemini 数据 → Step3 并行子系统 → 复制 Gemini → 汇总），不符合
"一条命令全自动按序执行全部 9 个子系统、每个子系统产出预测结果、实时监控"的诉求。v3.0 重构为 **单条 PIPELINE**。

### 8.2 执行顺序（严格按用户指定）
data → 双层LSTM → 顺口溜 → 重点点位分析 → 定金选2-分析 → KillSeeker → 点位期数-追踪 → gemini选2-预测 → 数据汇总复盘

### 8.3 架构要点
- **后端 `kl8-pwa/app.py`（v3.0）**：
  - 单一 `PIPELINE` 常量（9 系统，含 id/name/dir/timeout/icon/color/pre_cmds/cmd/post_cmds/outputs glob）。
  - `execute_pipeline(state, force)`：顺序串行执行；前一系统完成自动启动下一系统；每系统结束 `_detect_outputs` 检测结果文件并推 `sub_outputs`。
  - `_run_single_cmd` 统一注入 `PYTHONUNBUFFERED=1 / PYTHONIOENCODING=utf-8 / PYTHONUTF8=1 / KL8_NON_INTERACTIVE=1`，`force=True` 追加 `KL8_FORCE_RERUN=1`；`state.current_proc` 登记进程树，供「一键停止」taskkill /F /T。
  - 跳过检测：主命令输出含「每日校验 + 跳过」（同行或相邻行）→ 标记 `skipped`（⏭️ 今日已执行）。
  - SSE 事件：`snapshot / log / sub_log / progress / sub_status / sub_outputs / pipeline_status / heartbeat`。
  - 移除 v2 的 step2/3/5 手工编排、gemini 粘贴输入、step3-retry 等路由与执行器。
- **前端 `kl8-pwa/templates/index.html`（v3.0）**：
  - 「一键执行」卡片：启动/停止按钮、强制重跑勾选、总进度条、最新点位条。
  - 9 子系统管道列表：一行一系统，可展开该系统的实时子日志，状态徽章（待运行/执行中⏳/成功✅/失败❌/跳过⏭️）。
  - 主日志流 + 预测汇总卡片（9 系统解析结果 + 共识号码，一键复制/导出 txt）。
- **复用 §7 每日幂等守卫**：非交互默认跳过今日已执行系统；勾选「强制重跑」则全系统注入 `KL8_FORCE_RERUN=1` 真实重算。

### 8.4 本轮修复
- 跳过检测初期全判 `done`：guard 的跳过文案「每日校验」与「跳过」分占两行，app.py 同行匹配漏判。
  修复：`daily_run_guard.py` 跳过文案改为单行含「跳过本次执行」标记；app.py 检测兼容同行/相邻行双排版。

### 8.5 验证证据
1. `python -m py_compile kl8-pwa/app.py daily_run_guard.py` ✅。
2. 冒烟（session smoke2，非强制）：9 系统全部 **skipped**（今日均已执行）、30s 内完成、`pipeline_status: done`；
   `system_outputs` 各系统输出文件正确检测（如 abc → latest_predict.txt；aggregate → 分区深度聚合推荐_20260806.txt）。
3. 强制路径：`KL8_FORCE_RERUN=1 KL8_NON_INTERACTIVE=1 python data/main_v2.py --top 20`
   打印「♻️ [每日校验] data: 检测到 KL8_FORCE_RERUN=1，强制重跑」并真实重算（19:39 完成）。
4. API：`/api/subsystems` 返回 9 系统（用户顺序）；`/api/predictions` 聚合 9 解析器 + 共识号码；
   `/api/daily-points` 解析 2026208 期点位；`/api/session/<id>` 返回 pipeline_status/subsystem_status/system_outputs。
5. 前端 `index.html` 200 OK（78KB），JS 通过 `node --check`。

### 8.6 使用方式
```text
启动:  cd kl8-pwa && python app.py        →  http://localhost:5888
一键:  页面点「▶ 一键执行」；勾选「强制重跑」可覆盖今日已执行记录
停止:  「■ 停止」→ taskkill /F /T 结束当前子进程树
结果:  页面「预测汇总」复制/导出 txt；每个子系统输出文件在下述 outputs 字段指向位置
```

---

## 九、四类「有意识简化」接回流水线（本会话追加）

### 9.1 背景
用户确认将此前按「过于复杂可移除」授权简化的 4 类逻辑接回，PWA 流水线按每日分析脚本完整落地、不简化。

### 9.2 改动明细
1. **跨系统/内部提纯挂进入口（3 处 post_cmd，失败不阻断）**
   - data 后置 `内部提纯面板`：`scratch/today_console_panel.py`（任务4.5 共振分级/三维自洽/AUC 提纯，秒级）
   - 双层LSTM 后置 `跨系统提纯`：`scratch/purify_cross_validate.py`（一.5 前序系统交叉验证 → `outputs/reports/purify_{期}.txt`）
   - 重点点位 后置 `前序系统提纯报告`：`daily_report.py`（四.5 完整克隆报告 + 提纯段；接入 guard 独立 key「重点点位分析-提纯报告」，不与主预测连坐跳过）
2. **环境清理（共享函数 + 6 入口收尾）**：`daily_run_guard.py` 新增 `clean_pycache(root)`；顺口溜/data/双层LSTM/重点点位/点位期数/汇总复盘 6 入口在 `mark_daily_run_done` 后调用。KillSeeker/定金选2 原有清理不动。
3. **KillSeeker 形态引擎恢复**：`config/model_config.py` engine_weights 恢复 `pattern=0.15`，四项归一化 `similarity 0.25/density 0.25/pattern 0.15/curve 0.35`（总和 1.00）；main.py 过时说明文案同步更新。
4. **汇总复盘恢复 Gemini 链路（V18.4→V18.5，4 系统版）**
   - `collect_all` 恢复 `collect_from_gemini()`；解析器修复两缺陷：① 只抓「:」后到句号/行尾号码段（量化逻辑文本数字不再污染）；② section 按胆名+Top 区分（首席金胆/次席银胆/强力铜胆/Top1/Top2/Top3），旧实现 add_record 去重键全同导致每期只剩 1 个号码
   - `_resonance_tier` 恢复钻石档：`≥4 超级钻石 / 3 钻石 / 2 金级 / 1 铜级`（对齐话术矩阵在 4 系统架构下的映射）
   - 两处「禁止恢复钻石硬规则」话术改写；标题/文案 V18.4→V18.5
   - 保守决策：gemini **不纳入** ELITE_SYSTEMS（AI 源不进精英共识），只计入 systems 共振计数

### 9.3 验证证据
1. `python -m py_compile` 全部改动文件 ✅
2. guard 单测：clean_pycache 删 2 个目录 ✅；「重点点位分析-提纯报告」独立 key 首跑放行/二次跳过/force 重跑 ✅
3. `daily_report.py` 带跳过状态文件 → 打印跳过说明快速退出（不进入 section_zero）✅
4. 轻量冒烟：`purify_cross_validate.py` 秒级产出 `purify_2026208.txt` ✅；`today_console_panel.py` 面板输出 ✅；`KillSeeker main.py --full` 四引擎全跑（引擎3 形态识别 TOP3模板/填充率正常，贡献 13.1%，无「权重0·已跳过」）✅
5. `aggregate_v18.py` 真跑（临时状态文件）：采集层显示 4 系统，gemini 采集 **347 条**（6 版块），`⭐️⭐️⭐️ 钻石 (3系×4): 12 53 34 50` 钻石档触发，无解析崩溃 ✅
6. PWA 冒烟（smoke3）：9 系统主命令 skipped，3 条 post_cmd 全部真实执行（data 面板 0.9s / lstm 提纯 0.6s / points daily_report 17.1s 完整跑含环境清理），`重点点位分析` 目录 __pycache__ 清理为 0 ✅
7. 正式状态文件新增 `重点点位分析-提纯报告: 2026208 @ 20:19:47 [ok]` ✅；9 系统正式记录未受临时状态文件污染 ✅
8. 不 git commit ✅
