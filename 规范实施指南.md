# 工作空间规范实施指南

## 📋 概述

本指南详细说明如何在工作空间中实施和执行开发规范。通过一系列工具和流程，确保所有项目都符合统一的架构和质量标准。

## 🏗️ 规范体系架构

### 核心规范文件
1. **`WORKSPACE_RULES.md`** - 工作空间全局规范
2. **`3d/docs/specifications/CODING_STANDARDS.md`** - 3D项目编码规范（示例）

### 规范执行工具
1. **`check_all_projects.py`** - 批量项目合规性检查
2. **`check_compliance.py`** - 单个项目合规性检查（每个项目都应包含）
3. **`setup_git_hooks.py`** - Git预提交钩子安装
4. **`monitor_compliance.py`** - 规范执行监控系统
5. **`init_workspace.py`** - 工作空间初始化工具

### 支持文件
1. **`pre-commit-template.sh`** - Git预提交钩子模板
2. **`规范实施指南.md`** - 本文件

## 🚀 快速开始

### 1. 初始化工作空间
```bash
# 为新环境或新成员初始化
python init_workspace.py --all
```

### 2. 检查所有项目合规性
```bash
# 运行全面检查
python check_all_projects.py --report compliance_report.json

# 自动修复可修复的问题
python check_all_projects.py --fix

# 为缺少检查脚本的项目创建模板
python check_all_projects.py --create-templates
```

### 3. 设置Git预提交钩子
```bash
# 为所有项目安装钩子
python setup_git_hooks.py --all --force

# 为单个项目安装钩子
python setup_git_hooks.py 项目路径 --force
```

### 4. 设置规范监控
```bash
# 运行每日检查
python monitor_compliance.py --daily --report

# 生成每周总结
python monitor_compliance.py --weekly --report
```

## 📁 标准项目结构要求

### 必须的目录结构
```
项目名称/
├── src/                    # 源代码目录
│   ├── scripts/           # 可执行脚本（必须）
│   ├── frontend/          # 前端代码（如有）
│   ├── core/              # 核心功能
│   ├── models/            # 预测/分析模型
│   └── utils/             # 工具函数
├── docs/                  # 文档目录（必须）
├── tests/                 # 测试目录（必须）
├── outputs/               # 输出目录（必须）
│   ├── visualizations/    # 可视化结果
│   ├── predictions/       # 预测结果
│   ├── reports/           # 分析报告
│   └── logs/              # 运行日志
├── configs/               # 配置文件目录
├── data/                  # 数据目录
├── main.py               # 主入口脚本（推荐）
├── README.md             # 项目说明（必须）
├── requirements.txt      # Python依赖（Python项目必须）
└── .gitignore           # Git忽略文件（必须）
```

### 根目录文件禁令
**严禁**在根目录生成以下类型文件：
- 图片文件：`.png`, `.jpg`, `.jpeg`, `.gif`, `.bmp`, `.ico`
- 配置文件：`.json`, `.yaml`, `.yml`, `.toml`, `.ini`, `.cfg`
- 批处理脚本：`.bat`, `.cmd`, `.ps1`, `.sh`
- 临时日志文件：`.log`, `.tmp`, `.temp`, `.cache`
- 数据文件：`.csv`, `.xlsx`, `.xls`, `.parquet`, `.feather`
- 脚本文件：`.py`（除`main.py`和`setup.py`外）

## 🔧 开发工作流

### 新项目创建流程
1. **项目初始化**
   ```bash
   mkdir 新项目
   cd 新项目
   python ../init_workspace.py --project .
   ```

2. **开发规范检查**
   ```bash
   # 开发过程中定期检查
   python src/scripts/check_compliance.py
   
   # 自动修复问题
   python src/scripts/check_compliance.py --fix
   ```

3. **代码提交**
   ```bash
   git add .
   git commit -m "提交信息"
   # 预提交钩子会自动运行规范检查
   ```

### 现有项目迁移流程
1. **备份项目**
2. **运行初始化工具**
   ```bash
   python init_workspace.py --project 项目路径
   ```
3. **移动文件到正确目录**
4. **更新导入路径和配置**
5. **运行合规性检查并修复问题**
6. **测试功能是否正常**

## 📊 监控与报告

### 每日监控
```bash
# 运行每日检查并生成报告
python monitor_compliance.py --daily --report

# 查看生成的报告
ls compliance_reports/daily_report_*.html
```

### 每周总结
```bash
# 生成每周总结
python monitor_compliance.py --weekly --report

# 查看趋势分析
cat compliance_reports/weekly_summary_*.json | jq '.project_stats'
```

### 自定义监控计划
可以设置定时任务（如cron或Windows任务计划）自动运行监控：
```bash
# 每天上午9点运行
0 9 * * * cd /path/to/workspace && python monitor_compliance.py --daily --report
```

## 🛠️ 故障排除

### 常见问题

#### 1. 检查脚本执行失败
**问题**：`python src/scripts/check_compliance.py` 报错
**解决**：
```bash
# 重新创建检查脚本
python check_all_projects.py --create-templates --project 项目路径
```

#### 2. Git钩子不生效
**问题**：提交时没有运行规范检查
**解决**：
```bash
# 重新安装钩子
python setup_git_hooks.py 项目路径 --force

# 检查钩子文件权限
chmod +x .git/hooks/pre-commit
```

#### 3. 项目结构不符合规范
**问题**：检查报告显示目录缺失或文件位置错误
**解决**：
```bash
# 自动修复目录结构
python src/scripts/check_compliance.py --fix

# 手动移动文件到正确目录
mv *.png outputs/visualizations/
mv *.json configs/
mv *.py src/scripts/  # 除main.py外
```

#### 4. 监控报告生成失败
**问题**：`monitor_compliance.py` 报错
**解决**：
```bash
# 检查Python版本
python --version  # 需要3.8+

# 检查工作空间路径
python monitor_compliance.py --workspace .

# 查看详细错误信息
python monitor_compliance.py --daily 2>&1 | tee error.log
```

## 🔄 规范更新流程

### 1. 规范修改
1. 更新 `WORKSPACE_RULES.md`
2. 更新相关检查脚本
3. 测试规范变更

### 2. 通知与培训
1. 通知所有项目负责人
2. 更新本实施指南
3. 组织规范培训

### 3. 逐步实施
1. 为新项目立即应用新规范
2. 为现有项目设定整改期限
3. 监控规范执行情况

### 4. 反馈与优化
1. 收集实施反馈
2. 优化规范内容
3. 更新工具和流程

## 📈 成功指标

### 量化指标
1. **项目合规率**：符合规范的项目比例
2. **问题修复时间**：从发现问题到修复的平均时间
3. **规范检查通过率**：代码提交时检查通过的比例
4. **监控覆盖率**：被监控的项目比例

### 质量指标
1. **代码一致性**：项目间代码风格和结构的一致性
2. **维护便利性**：项目维护和迁移的便利程度
3. **新人上手时间**：新成员理解项目结构的时间
4. **团队协作效率**：团队成员协作的效率提升

## 📞 支持与资源

### 文档资源
1. **规范文档**：`WORKSPACE_RULES.md`
2. **实施指南**：本文件
3. **示例项目**：`3d/` 项目作为规范实施示例
4. **工具文档**：各工具脚本的文档字符串

### 技术支持
1. **工具问题**：查看工具脚本的错误输出
2. **规范疑问**：参考规范文档和示例
3. **实施困难**：联系架构委员会或规范负责人

### 培训资源
1. **新成员培训**：运行 `init_workspace.py` 和阅读本指南
2. **规范培训**：定期组织规范培训和分享
3. **最佳实践**：收集和分享规范实施最佳实践

## 🎯 总结

通过本规范实施体系，您可以：

1. **统一工作空间架构**：所有项目遵循相同的结构标准
2. **自动化规范检查**：通过工具自动执行规范检查
3. **预防规范违规**：通过预提交钩子防止违规代码提交
4. **持续监控改进**：通过监控系统持续改进规范执行
5. **提升开发效率**：通过标准化提升团队协作效率

**记住**：规范不是限制，而是提升质量和效率的工具。合理的规范应该促进而不是阻碍开发工作。

---

**最后更新**：2026年1月30日  
**适用范围**：`D:\Dpanqianyi\Python-Project\` 工作空间  
**维护责任**：架构委员会和所有项目负责人